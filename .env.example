# Optional: enables LLM-augmented answer generation.
# Without this, TrustLoop runs in fully deterministic offline mode
# using TF-IDF retrieval and template composition.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
